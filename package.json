{
  "name": "mathmania",
  "workspaces": [
    "apps/*"
  ],
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.3"
  },
  "packageManager": "npm@10.0.0"
}
